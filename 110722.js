var summation = function (num) {
  let sum = 0. //create an empty sum
  for( let i = 0; i <= num; i++){ //start a loop that begins at zero and goes until it equals the inputted num
      sum += i //add and total all numbers i the loop to the sum
   
  }
  return sum // return
  
}
